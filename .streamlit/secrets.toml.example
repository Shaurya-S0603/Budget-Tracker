# Copy into Streamlit Cloud -> App settings -> Secrets.
# Replace this placeholder with the connection URL from your PostgreSQL provider.
# Keep real passwords in Secrets, never in GitHub.
[database]
url = "postgresql://USER:PASSWORD@HOST/DATABASE?sslmode=require"
